id: jigeum-reports-list
name: "Jigeum Reports List"
description: "Verify Jigeum can list generated reports"

agent:
  type: command
  command: "cd /Users/yongrean/Downloads/nightbot && npx tsx src/cli.ts reports"

steps:
  - action: check_output
    expect: "report"

evaluate:
  method: rules
  passThreshold: 50
  rules:
    - type: exit_code
      target: exit
      value: "0"
      weight: 2
    - type: regex
      target: stdout
      value: "(report|\\.md|No reports)"
      weight: 1
